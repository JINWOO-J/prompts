services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db-data:/app/data
    env_file:
      - .env
    environment:
      - PROMPTS_DB_PATH=/app/data/prompts.db
    restart: unless-stopped

volumes:
  db-data:
